// premitive
// 7 tyeps:- string, number, boolearn, null, undefined, bigint, symbol

const score = 100;
const scorevalue = 110.23;

const isloggedin = false;
const outtemp = null;
let userid;
console.log(isloggedin === outtemp);
const bignum = 12412413432

// non premitive (refrence)
// array, objects, functions

const hero = "superman"
let myobj = {
    name : suyash
}
